name: trivy-scan

on:
  push:
  pull_request:
  # run weekly to pick up any new CVEs
  schedule:
    - cron: '30 2 * * 1' # 0230 UTC, 2130 EST on Monday nights

jobs:
  scan:
    runs-on: ubuntu-latest
    # avoid duplicate jobs on PRs from the main repo
    if: github.event_name == 'schedule' || github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name

    steps:
      - uses: actions/checkout@ac593985615ec2ede58e132d2e21d2b1cbd6127c # v3.3.0
      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '8'
          cache: 'maven'
      - name: Build with Maven
        run: mvn clean install -B -Dhttp.keepAlive=false -Dmaven.wagon.http.pool=false -DskipTests
      - name: Extract NARs
        run: >-
          for file in $(find . -name '*.nar'); do
            rm -r $(dirname "$file")/META-INF
            unzip -qd $(dirname "$file") "$file"
          done
      - name: Create output directory
        run: mkdir sarif
      - name: Trivy geomesa-accumulo20-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-accumulo20-nar.sarif'
          scan-ref: './geomesa-accumulo-bundle/geomesa-accumulo20-nar/target/META-INF/'
      - name: Trivy geomesa-accumulo21-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-accumulo21-nar.sarif'
          scan-ref: './geomesa-accumulo-bundle/geomesa-accumulo21-nar/target/META-INF/'
      - name: Trivy geomesa-datastore-services-api-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-datastore-services-api-nar.sarif'
          scan-ref: './geomesa-datastore-bundle/geomesa-datastore-services-api-nar/target/META-INF/'
      - name: Trivy geomesa-datastore-services-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-datastore-services-nar.sarif'
          scan-ref: './geomesa-datastore-bundle/geomesa-datastore-services-nar/target/META-INF/'
      - name: Trivy geomesa-fs-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-fs-nar.sarif'
          scan-ref: './geomesa-fs-bundle/geomesa-fs-nar/target/META-INF/'
      - name: Trivy geomesa-gt-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-gt-nar.sarif'
          scan-ref: './geomesa-gt-bundle/geomesa-gt-nar/target/META-INF/'
      - name: Trivy geomesa-hbase1-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-hbase1-nar.sarif'
          scan-ref: './geomesa-hbase-bundle/geomesa-hbase1-nar/target/META-INF/'
      - name: Trivy geomesa-hbase2-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-hbase2-nar.sarif'
          scan-ref: './geomesa-hbase-bundle/geomesa-hbase2-nar/target/META-INF/'
      - name: Trivy geomesa-kafka-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-kafka-nar.sarif'
          scan-ref: './geomesa-kafka-bundle/geomesa-kafka-nar/target/META-INF/'
      - name: Trivy geomesa-lambda-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-lambda-nar.sarif'
          scan-ref: './geomesa-lambda-bundle/geomesa-lambda-nar/target/META-INF/'
      - name: Trivy geomesa-redis-nar
        uses: aquasecurity/trivy-action@1f0aa582c8c8f5f7639610d6d38baddfea4fdcee # 0.9.2
        with:
          scan-type: 'rootfs'
          vuln-type: 'library'
          format: 'sarif'
          output: 'sarif/geomesa-redis-nar.sarif'
          scan-ref: './geomesa-redis-bundle/geomesa-redis-nar/target/META-INF/'
      - name: Fix library paths
        run: >-
          for file in $(ls sarif); do
            sed -i "s|library/|${file%.sarif}/|" "sarif/$file"
          done
      - name: Upload sarifs
        uses: github/codeql-action/upload-sarif@17573ee1cc1b9d061760f3a006fc4aac4f944fd5 # v2.2.4
        with:
          sarif_file: 'sarif'
      - name: Remove GeoMesa artifacts
        run: |
          rm -rf ~/.m2/repository/org/geomesa/nifi